# Tiny Weather CLI

A small demo project after running GainAgent Release Kit.

## What Changed

The project now has:

- GitHub Actions workflow for release notes
- `git-cliff` configuration
- release checklist

Command used:

```bash
node gainagent-release-kit/bin/gainagent-release-kit.js init --target examples/demo-project-after
```
